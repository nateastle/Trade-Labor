<%= form_for [current_user, @photo] , :html =>{ :class => "upload_fornm"} , :validate => true do |f| %>
  <% if @photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@painting.errors.count, "error") %> prohibited this picture from being saved:</h2>
      <ul>
      <% @photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>

    <%= f.file_field :name %>
  </p>
  <div class="actions">
    <%= f.submit "Upload Photo" %>
  </div>
<% end %>