<div class="profile_cel3">
  
  <!-- About My Company Start -->
  <div class="company_box">
  	  <h2 class="heading_t"><span></span>About My Company</h2>
	  <div class="psl_text">
	    <p>
	      Lorem Ipsum, giving tion on its origins,
	      Lorem Ipsum, giving infomation Ipsum, giving infomation on its origins Lorem Ipsum, giving infomationLorem Ipsum, giving infomation
	    </p>
	    <p>
	      Giving tion on its origins,
	      Lorem Ipsum, giving infomation Ipsum, giving infomation on its origins Lorem Ipsum, giving infomationLorem Ipsum, giving infomation 
	    </p>
	  </div>
  </div>
  <!-- About My Company Start -->

  <!-- Feed back Form Start -->
	  <% if current_user && (current_user.id != @user.id) && !current_user.already_rated?(@user) %>
	  <%# if current_user && (current_user.id != @user.id)  %>
	   <%# if current_user.already_rated?(@user) %>
	   	 <%#= render :partial => 'display_feedback_per_business' , :locals=>{:rating_per_criteria_hash =>rating_per_criteria(@business),:business=> @business } %>
	   <%#else%>	
			<%= render :partial => 'form_for_feedback'%>
	   <%# end %>	
	  <% end %> 
  <!-- Feed back Form Start -->

  <% unless @rated_business_array.blank? %>
			<!-- Feedbacks Start -->
			<h2 class="heading_t"><span></span> Feedback’s</h2>

			<div class="cmt_box">
					<% @rated_business_array.each do |business| %>
				   	 <%= render :partial => 'display_feedback_per_business' , :locals=>{:rating_per_criteria_hash => rating_per_criteria(business) , :business => business }%>
				  <% end %>	
			</div>
			<!-- Feedbacks Start -->

			<div class="clear"></div>

			<div class="see_more">
				<a href="#" class="btn_r2">See More Feedbacks</a>
			</div>
	<% end %>		
</div>
